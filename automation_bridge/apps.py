from django.apps import AppConfig


class AutomationBridgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "automation_bridge"
    verbose_name = "Automation Bridge"
